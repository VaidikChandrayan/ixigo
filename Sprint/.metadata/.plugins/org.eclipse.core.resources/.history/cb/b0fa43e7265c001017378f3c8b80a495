package com.pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;

public class TrainInvalidData extends BasePage{
	
	 public TrainInvalidData(WebDriver driver) {
		super(driver);
		// TODO Auto-generated constructor stub
	}
	 
	 @FindBy(xpath="//p[contains(text(),\"Mumbai - All stations(LTT)\")]")
	 WebElement sugg;
	public void enterStationDetails(String from, String to) {
		
			FromValid.click();
	        
	        FromValid.sendKeys(from);
	        
	        ToValid.click();
	        
	        ToValid.sendKeys(to);
	        //sugg.click();
	        actions.moveToElement(sugg).click().build().perform();
	    }


}
